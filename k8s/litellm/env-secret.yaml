apiVersion: v1
kind: Secret
metadata:
  name: litellm-env
  namespace: rag-platform
type: Opaque
stringData:
  # PROXY_MASTER_KEY: set this before applying — use a random 32-char string
  # REDIS_URL: rediss://rag-platform-redis-shkgzd.serverless.apse2.cache.amazonaws.com:6379
  PROXY_MASTER_KEY: "REPLACE_ME"
  REDIS_URL: "REPLACE_ME"
